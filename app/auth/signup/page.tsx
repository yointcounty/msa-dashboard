'use client';
import Image from 'next/image'; import Link from 'next/link'; import { useRouter } from 'next/navigation'; import { useState } from 'react'; import { ArrowLeft, ArrowRight, Check } from 'lucide-react'; import { createAccount } from '../../lib/auth';
export default function SignupPage(){const router=useRouter();const[status,setStatus]=useState('');const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setStatus('');const d=new FormData(e.currentTarget),password=String(d.get('password')),confirm=String(d.get('confirm'));if(password!==confirm){setStatus('Passwords do not match.');return}setBusy(true);await createAccount({parentName:String(d.get('parentName')),email:String(d.get('email')),childName:String(d.get('childName')),program:String(d.get('program')),password});router.push('/dashboard')}
return <main className="auth-shell"><section className="auth-visual"><Image src="/images/msa-coaching.jpg" alt="Miami Skate Academy skaters and coaches" fill priority/><div className="auth-visual-copy"><h2>THEIR NEXT<br/><em>WIN STARTS HERE.</em></h2><p><Check size={17}/> All skill levels welcome &nbsp; <Check size={17}/> Small coach-led groups</p></div></section><section className="auth-panel"><Link href="/" className="brand-lockup"><span className="brand-mark">MSA</span><span><b>MIAMI SKATE</b><small>ACADEMY</small></span></Link><Link href="/" className="back-link"><ArrowLeft size={15}/> Home</Link><div className="auth-card"><p className="eyebrow"><span/> Join 100+ members</p><h1>ENROLL.</h1><p>Create your parent access and tell us a little about your skater.</p><form onSubmit={submit}><div className="form-grid"><div className="field"><label htmlFor="parentName">Parent name</label><input id="parentName" name="parentName" required autoComplete="name"/></div><div className="field"><label htmlFor="childName">Skater name</label><input id="childName" name="childName" required/></div></div><div className="field"><label htmlFor="email">Parent email</label><input id="email" name="email" type="email" required autoComplete="email"/></div><div className="field"><label htmlFor="program">Program interest</label><select id="program" name="program"><option>Private Lessons</option><option>Small Group Lessons</option><option>Travel Team</option><option>Seasonal Camp</option></select></div><div className="form-grid"><div className="field"><label htmlFor="password">Password</label><input id="password" name="password" type="password" minLength={8} required autoComplete="new-password"/></div><div className="field"><label htmlFor="confirm">Confirm password</label><input id="confirm" name="confirm" type="password" minLength={8} required autoComplete="new-password"/></div></div>{status&&<div className="form-status error" role="alert">{status}</div>}<button className="button" disabled={busy}>{busy?'Creating access…':<>Complete enrollment <ArrowRight size={18}/></>}</button></form><p className="auth-switch">Already enrolled? <Link href="/auth/login">Sign in</Link></p></div></section></main>}
